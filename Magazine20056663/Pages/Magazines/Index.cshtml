@page
@model Magazine20056663.Pages.Magazines.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Magazine[0].magazineName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Magazine[0].Price)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Magazine) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.magazineName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.magazineID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.magazineID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.magazineID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
